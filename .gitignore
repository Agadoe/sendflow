# Dependencies
node_modules/

# Next.js build output
.next/
.next/trace
tsconfig.tsbuildinfo

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local
.env.deployed
.env.vercel
.env.bak
.env.production

# Local databases
*.db
*.db-journal

# WhatsApp session data (contains auth tokens and QR codes)
.wwebjs_auth/
.wwebjs_cache/

# Runtime logs
*.log
loop.log

# Vercel
.vercel
vercel.json

# OS files
.DS_Store

# Test outputs
test-results/
playwright-report/

# Waitlist / user data exports (PII)
*.csv
sendflow-waitlist.csv

# Build artifacts (added 2026-06-05)
.next/trace
tsconfig.tsbuildinfo

# Local secrets (must never be committed)
.env
.env.bak
.env.local
.env.prod
MEMORY-BUGS.md
.env*.local

# Extra env patterns (added 2026-08-11 — captured real creds on stage)
.env.production.new
.env.v
